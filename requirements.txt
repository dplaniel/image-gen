# PyTorch with CUDA 13.2 support — install via setup.sh, not pip directly
# torch==2.12.0+cu132  (from https://download.pytorch.org/whl/cu132)

diffusers>=0.33.0
transformers>=4.51.0
accelerate>=1.6.0
bitsandbytes>=0.46.0
sentencepiece>=0.2.0
protobuf>=5.0.0
Pillow>=11.0.0
huggingface_hub>=0.30.0
